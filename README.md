# HTML + JavaScript Project

A simple static site with HTML, CSS, and JavaScript.  
Live preview button uses a basic `onclick` handler.

## How to Use

1. Open `index.html` in a browser
2. Or host it with GitHub Pages for live deployment

## GitHub Pages

- Go to your repo > Settings > Pages
- Set source to: `main` branch, `/ (root)`
